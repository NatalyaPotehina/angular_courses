export class About {
  readonly photoSourse: string;
  readonly textAbout: string;

  constructor(photoSours: string, textAbout: string) {
    this.photoSourse = photoSours;
    this.textAbout = textAbout;
  }
}
